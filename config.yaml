# Tenant Configuration: Demo Corporation
tenant_id: "tenant_001_demo"
company_name: "Tech Flow Inc."

# --- POLICY ENGINE ---
# Rules that change from company to company
policies:
  currency_base: "USD"
  auto_approve_limit: 1000.00  
  require_po_match: true      
  allow_partial_pay: false
  payment_terms_default: "NET30" 

# --- INTEGRATIONS ---
# Switchable drivers (e.g., swap 'slack' for 'teams')
integrations:
  notification_channel: "slack"
  payment_gateway: "stripe"
  accounting_software: "csv_ledger" 

# --- COMPLIANCE ---
compliance:
  check_fraud_lists: true
  tax_region: "US"